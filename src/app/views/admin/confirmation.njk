<script>
    const deleteBtn = document.querySelector('button.delete')

    deleteBtn.addEventListener('click', function (e) {
        const confirmation = window.confirm("Deseja realmente deletar a receita {{ recipe.title }}?")

        if (!confirmation) {
            e.preventDefault()
        }
    })
</script>